container_commands:
  installGrunt:
    command: $NODE_HOME/bin/npm install -g grunt-cli
    test: $NODE_HOME/bin/npm --version
  installGrunt:
    command: $NODE_HOME/bin/npm install -g sails
    test: $NODE_HOME/bin/npm --version
